---
collections:
  - name: azure.azcollection
  # community.general provides archive/redhat_subscription, community.postgresql
  # provides the database dump/restore used by the backup and restore playbooks.
  - name: community.general
  - name: community.postgresql
