---
collections:
  # Lower bounds are pinned so builds do not silently resolve to an old,
  # unpatched release; upper bounds keep major upgrades an explicit review.
  - name: azure.azcollection
    version: ">=3.21.0,<4.0.0"
  - name: community.general
    version: ">=13.3.0,<14.0.0"
  - name: community.postgresql
    version: ">=4.2.0,<5.0.0"
  - name: ansible.posix
    version: ">=2.2.2,<3.0.0"
